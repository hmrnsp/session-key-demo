PORT=4000

# Umur session key dalam detik. Default 1800 = 30 menit (sesuai konsep).
# Set kecil (mis. 20) untuk mencoba alur rotasi otomatis dengan cepat.
SESSION_TTL_SECONDS=1800

# Izinkan origin client demo (UI browser) untuk memanggil API ini.
CLIENT_ORIGIN=http://localhost:3000
